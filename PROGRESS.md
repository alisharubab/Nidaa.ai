# Progress Tracker

**Project:** Nidaa-AI
**How to use this file:** this is the single shared source of truth for "what's done and what's left." Task IDs match [docs/IMPLEMENTATION_PLAN.md](docs/IMPLEMENTATION_PLAN.md) — look a task up there for full context, then flip its checkbox here when it's done.

**Rules for keeping this useful:**
1. Check a box only when the task actually works end to end, not when the code is written. "Written but untested" stays unchecked.
2. Put your name/initial and the date in the Owner/Notes column when you pick up or finish a task — don't leave it blank, that's how two people end up building the same thing.
3. If you change scope on a task (skip it, descope it, discover it needs splitting), say so in Notes rather than silently leaving it unchecked forever.
4. Update this file in the same sitting you finish the work — not at end of day, not "later."

---

## North Star Metric

| Metric | Value | Last updated |
| :-- | :-- | :-- |
| Human baseline (median, ms) | 29940 ms (p95 33753 ms, n=6) — see caveat on DATA-03 | 2026-08-30 |
| System median TTT (ms) | 2207 ms (p95 18398 ms, n=19, live dashboard traffic) — informal live number, NOT the formal pitch figure; the deck number should come from the DATA-10 gold-set run | 2026-09-01 |
| System p95 TTT (ms) | 18398 ms (same measurement as above) | 2026-09-01 |
| Target | < 5000 ms median | |

---

## Day 0 — Accounts and access

| ID | Task | Done | Owner / Notes |
| :-- | :-- | :--: | :-- |
| SETUP-01 | Groq account + API key + model IDs confirmed | [x] | Ali, 2026-08-29 — key in .env, all 4 model IDs confirmed available on Groq console |
| SETUP-02 | Burner SIM acquired for WhatsApp | [x] | Ali — using personal SIM with WhatsApp on main phone |
| SETUP-03 | Node 20+, Python 3.11+, ffmpeg installed (both machines) | [x] | Ali — Node 20+, Python 3.12, ffmpeg 9.0.1 (via WinGet) |
| SETUP-04 | Branch strategy + merge owner agreed | [x] | Ali — direct push to master, small commits, TRD §2/§3 changes flagged via addendum |

## Day 1 — Contracts and scaffolding

| ID | Task | Done | Owner / Notes |
| :-- | :-- | :--: | :-- |
| CORE-01 | FastAPI app scaffold + health check | [x] | Alisha, 2026-08-29 — boots clean, `/health` returns real status |
| CORE-02 | `db.py`: tables from TRD §2 DDL, WAL pragmas, migration runner | [x] | Alisha — DDL + full DAO layer (messages/tickets/consent/metrics), tested via curl. Also: (1) `PRAGMA foreign_keys=ON` + real FK on `messages.sender_hash`; (2) split `verification_status`/`dispatcher_verdict`; (3) full CHECK-constraint pass on every enum/range/JSON-shaped column; (4) `geocode_score` normalized to always be 0-1 (fuzzy matches now store `WRatio/100.0`, not the raw 0-100 value) + range-checked, `audio_duration_s`/`ttt_ms` positivity-checked. 14 bad-data cases tested across both rounds, all correctly rejected with `IntegrityError`, full legit flow (incl. `/api/simulate`) still passes |
| CORE-03 | `events.py`: append-event helper, same-transaction writes | [x] | Alisha — verified via live SSE replay test |
| CORE-04 | Public routes stubbed with fixture JSON matching TRD §3.3 | [x] | Alisha — went further than the Day-1 stub: routes are real and DB-backed, not fixtures. Tickets stay empty until the pipeline (Day 2) or `/api/simulate` populates them |
| CORE-05 | Groq smoke test (STT + LLM, hardcoded input) | [x] | Alisha, 2026-08-30 — `core/smoke_test_groq.py`, run against a real edge-tts-generated Urdu voice note. **4/4 passing.** STT primary + escalation both transcribe real audio with confidence metrics in gate range; both LLMs produce perfectly-structured extractions. `LLM_MODEL_FALLBACK` (`qwen/qwen3.6-27b`) initially failed Groq's `json_object` mode outright (it's a reasoning model, emits `<think>` before answering) — root-caused via Groq's own Reasoning docs and fixed with two extra kwargs (`reasoning_format="hidden"`, `reasoning_effort="none"`), no model swap needed. Documented as a TRD §4.4 addendum + inline in `pipeline/extract.py` so `CORE-08`/`CORE-16` don't rediscover this the hard way |
| ING-01 | Baileys socket bootstrap, pairing-code login | [x] | Ali/Qoder, 2026-08-29 — full `startSock()` with `useMultiFileAuthState`, pairing-code login, reconnect on close, hard-stop on loggedOut, reply server on port 3000, jidMap for outbound routing. Tested: daemon boots, connects to WA socket, reply server listens. Pairing needs phone. Baileys logic now lives in `transports/whatsapp.js` (moved unchanged, one env-var flip away). |
| ING-02 | QR/pairing confirmed, inbound events logged | [x] | Ali/Qoder, 2026-08-30 — Green API WhatsApp transport (`ingest/transports/greenapi.js`). Baileys direct-socket pairing was blocked by WA rate-limiting; Green API HTTP gateway (QR scan once in console.green-api.com) used instead. `INGEST_TRANSPORT=greenapi`. Live-verified: text message received + processed, voice note received + WAV on disk + pipeline ran, readback fired, `1` reply → `user_confirmed`. Full correction loop confirmed on real WhatsApp. |
| ING-03 | `POST /internal/ingest` stub calls against CORE-04 | [x] | Ali/Qoder — wired inside `handleMessage()`, posts full payload shape per TRD §3.1, also includes audio path/duration from downloadAndConvertToWav. Round-trips re-confirmed live via Telegram transport: text + audio rows in DB. |
| FE-01 | Static dashboard shell (header/rails/map container) | [x] | Ali/Qoder, 2026-08-29 — full shell: brand mark + marigold dot in header, TTT metrics area, live indicator with pulsing dot, left rail with queue nav + district totals + HXL export button, glass filter bar over map, map legend, waveform-glyph empty state, presentation mode flag. Typography classes per UI-UX §3. |
| FE-02 | Mock ticket list from static JSON fixture | [x] | Ali/Qoder — full ticket card per UI-UX §6.2: 3px urgency bar, urgency badge, district+P-code, 2-line-clamped items summary, waveform placeholder bars, state dot (confirmed/disputed/unconfirmed), Acknowledge button with verdict POST, aria-labels. Queue counts auto-update. Verified live: /api/simulate → SSE → card renders. **Fixed by Alisha, 2026-08-30:** `verificationState()` in `app.js` checked `verification_status === "dispatcher_verified"`, a value the CORE-02 schema split removed — Acknowledge was silently not updating the state dot/pin. Now checks `dispatcher_verdict === "verified"` instead. Confirmed live: click Acknowledge → dot flips to Confirmed, pin goes solid. |
| DATA-01 | HDX COD-AB Pakistan gazetteer CSV built | [x] | Alisha, 2026-08-30 — downloaded the real HDX resource (`pak_admin_boundaries.xlsx`, WFP SDI COD-AB), built `tools/build_gazetteer.py` to flatten it into `data/pak_gazetteer.csv` per the exact TRD §4.5 column spec. 577 tehsils, 160 districts, 7 provinces, zero missing lat/lon/pcode, validated with a script. Also fixed `/api/simulate`'s Dadu P-code, which had been a guessed placeholder (`PK602`) — real value is `PK703`, now corrected with the real tehsil centroid too |
| DATA-02 | `aliases.json` seeded for top 40 districts | [x] | Alisha, 2026-08-30 — 78 alias entries covering 43 real flood-prone districts (Sindh/Balochistan/South Punjab/KP), every target cross-checked to actually exist in `pak_gazetteer.csv` (zero broken aliases). Canonical spellings pulled straight from HDX, which often differ from common usage — e.g. `Kambar Shahdad Kot`, `D. I. Khan`, `Shaheed Benazir Abad` |
| DATA-03 | Human baseline stopwatch run recorded | [x] | Alisha, 2026-08-30 — real timed run completed: median 29940ms / p95 33753ms / n=6, live in `data/human_baseline.json` and already flowing through `/api/metrics` (`human_baseline_ms`), confirmed via curl, no restart needed. **Caveat, not a blocker:** this number is well under PRD §2.1's expected 90-300s range — likely because the dispatcher (Alisha) already knew these 6 messages' content going in, answers were quick/rough rather than validated, and there was no map cross-reference or spreadsheet entry (the real friction PRD §1.2 describes). Treat as a placeholder; a colder, more representative run (ideally alongside `DATA-07`'s 25-message gold set) should replace it before this number goes in the pitch deck |
| — | *(unscheduled)* Telegram dev/test transport in `ingest/transports/` (`INGEST_TRANSPORT` env var) | [x] | Ali/Qoder, 2026-08-29 — adapter pattern: transport-agnostic handler in index.js, Baileys in `transports/whatsapp.js` (unchanged), core/ + TRD contracts untouched (ARCHITECTURE §4 updated). Live-tested via @nidaa_ai_bot: text + 3.9s voice round-trips (16kHz mono WAV on disk + DB rows), consent notice exactly once, BAND→revoked+purged, post-BAND silent drop, photo ignored, /start filtered, /internal/reply synthetic delivery. |
| — | **Sync point:** schema (TRD §2) + API contract (TRD §3) frozen | [ ] | Several changes since the original freeze, all documented as addenda in `docs/TRD.md` §2/§3.1: consent endpoints (new), FK enforcement + `verification_status`/`dispatcher_verdict` split, full CHECK-constraint pass, `geocode_score` normalized to 0-1. Still needs both of you to actually read and confirm together — flip this box once he has |

## Day 2 — Vertical slice

| ID | Task | Done | Owner / Notes |
| :-- | :-- | :--: | :-- |
| CORE-06 | `pipeline/preflight.py` | [x] | Alisha, 2026-08-30 — real ffprobe/ffmpeg calls, tested against 3 real fixtures: normal speech (passes), 0.5s clip (`AUDIO_TOO_SHORT`), 2s silence (`AUDIO_SILENT`, -91dB measured) |
| CORE-07 | `pipeline/stt.py` (no gate yet) | [x] | Alisha — real Groq Whisper call, tested against real edge-tts audio, returns text/language/segments/model as a plain dict |
| CORE-08 | `pipeline/extract.py` (no fallback retry yet) | [x] | Alisha — real Groq call + Pydantic `ExtractionResponse` validation. Also built the fallback-retry path (pulled forward from `CORE-16`, see below) since it was trivial once `extract()` existed. **Anti-hallucination guard tested directly:** `ExtractionRecord` uses `extra="forbid"`, confirmed a payload containing `latitude`/`longitude` is rejected by Pydantic instead of silently ignored — a model trying to invent coordinates now fails validation and triggers the fallback/failed path, per hard rule 3 |
| CORE-09 | Stages wired synchronously, one hardcoded test file end to end | [x] | Alisha — real `/internal/ingest` now runs preflight→stt→extract inline (`_run_pipeline_sync` in `main.py`). Tested live: (1) multi-district text message → 2 separate tickets, both correctly Unlocated (no geocoder yet — intentional, see hard rule 3); (2) real audio message → full status trail `received→transcribed→extracted`, `ttt_ms` computed, transcript+STT confidence metrics now persisted to `raw_text`/`stt_*` columns (was a real gap, found and fixed while verifying); (3) silent audio → `preflight_failed`/`AUDIO_SILENT`, zero tickets, zero API calls made; (4) SSE event trail confirmed to capture the full lifecycle for both the success and failure paths. **Real finding along the way:** a garbled STT transcript (edge-tts mispronunciation) produced *confident-looking* gate metrics (avg_logprob -0.49) while being semantically nonsense — Whisper can be confidently wrong, which is exactly why `DATA-07` requires real human recordings, not just TTS, for any accuracy claim |
| CORE-10 | Real `GET /api/stream` SSE (replaces stub) | [x] | Alisha — tested with curl AND live in-browser, reconnect replay confirmed |
| ING-04 | `media.js`: download + ffmpeg convert to 16kHz mono wav | [x] | Ali/Qoder, 2026-08-29 — `convertToWav` was done by Person A; `downloadAndConvertToWav` now implemented in index.js: downloads via `downloadMediaMessage`, writes temp file, converts, cleans up, returns path + duration. Live-verified via Telegram: 3.9s ogg/opus → PCM 16kHz mono WAV. |
| ING-05 | Real `POST /internal/ingest` payload wired to real core routes | [x] | Ali/Qoder — fully wired in `handleMessage()`, same as ING-03. Real payload with sender_hash, phone_tail, audio_path, modality, duration, text, received_at. |
| FE-03 | Real SSE client rendering live `ticket.created` events | [x] | Alisha — verified live in browser: ticket card + map pin both rendered from a real SSE event, screenshot confirmed |
| FE-04 | Basic Leaflet map, one pin per ticket | [x] | Confirmed live — pin landed exactly on Dadu's coordinates |
| DATA-04 | Alias table extended toward full top-40 | [x] | Already satisfied by `DATA-02`'s work (2026-08-30) — 43 districts, exceeds the top-40 target, every target verified against the real gazetteer |
| — | **Sync point:** one real voice note, phone-to-pin, run together | [x] | Ali/Qoder, 2026-08-30 — confirmed live on real WhatsApp via Green API: voice note (Dadu, water+food) → STT → extract → geocode (exact, PK703) → urgency (high) → readback fired → `1` reply → `user_confirmed`. Ticket #7 in DB. |

## Day 3 — Real ingestion plus resilience

| ID | Task | Done | Owner / Notes |
| :-- | :-- | :--: | :-- |
| CORE-11 | `pipeline_queue.py`: asyncio queue + worker pool + token buckets | [x] | Alisha, 2026-08-30 — real `PipelineQueue` (generic, handler-based per ../CLAUDE.md's "main.py is the only orchestrator"), wired into `/internal/ingest` and `/api/simulate`. Tested: 3 concurrent messages processed in parallel, `queue_depth` in responses/`/api/metrics` is now literally accurate (was a placeholder since Day 2) |
| CORE-12 | Retry/backoff + 429 bucket-halving | [x] | Alisha — `call_with_retry()`: exponential+jitter backoff, blocking Groq calls run via `asyncio.to_thread` (they'd otherwise block every worker, not just one). Tested with a controlled fake `RateLimitError`: 3 attempts, ~3.55s of real backoff delay, bucket refill correctly halved twice (1000→500→250 across two simulated 429s) |
| CORE-13 | STT confidence gate (no_speech/logprob/compression/tokens) | [x] | Alisha — tested against real degraded audio (synthetic pink noise via `DATA-05`, no download needed): clean sample passes; -10dB SNR fails primary, escalation survives at the edge (avg_logprob -0.87 vs -0.90 threshold); -25dB SNR fails both models, caught specifically by the content-length check (1-word hallucinated transcript) rather than the numeric thresholds — confirms all four gate conditions are load-bearing, not just the obvious ones |
| CORE-14 | Escalation path → `audio_unintelligible`, skip extraction | [x] | Alisha — confirmed live: primary fails → escalation attempted → still fails → `status=audio_unintelligible`, `error_code=STT_LOW_CONFIDENCE`, zero tickets created, `extract()` never called. Also built the outbound `POST /internal/reply` call to ingest/ (wasn't wired anywhere before) — tested that it fails gracefully (logs, doesn't crash the worker) when ingest/ isn't running |
| CORE-15 | `queue_depth` on metrics SSE event, every 2s | [x] | Alisha — added to `/api/stream`'s existing 2s poll loop as a live-only `event: metrics` broadcast (not persisted to `events`, since it's a snapshot not replayable history). Confirmed firing via curl |
| ING-06 | `consent.js`: consent ledger state machine, BAND/STOP | [x] | Alisha — implemented against the new `/internal/consent/*` endpoints, tested standalone with `node -e` (no WhatsApp needed): first-contact notice, no-resend, BAND→revoked all confirmed. Ali/Qoder — live-confirmed end-to-end via Telegram 2026-08-29: notice exactly once, BAND→revoked + raw_text/audio_path purged in DB, post-revoke silent drop. |
| ING-07 | `outbound.js`: single reply queue, jittered pacing | [x] | Ali/Qoder, 2026-08-29 — live-confirmed: consent notice + reply-server message both delivered through the single jittered queue (1.5–3.0s); pump hardened so a failed send can no longer stall the queue. |
| ING-08 | `templates.js`: consent/readback/unintelligible/location copy | [~] | Update 2026-09-01: consent_notice, readback and location_missing all live-delivered on real WhatsApp via Green API ("jsjos"/"hello" → location_missing; Dadu voice note → readback → `1` → confirmed). audio_unintelligible: outbound wiring proven end to end (core pipeline → `POST /internal/reply` → ingest reply server received it during the DoD-3 noise test), and template content live-delivered via Telegram earlier — but actual WhatsApp delivery to a real phone still awaits a genuinely noisy voice note from a real sender (re-verify during DATA-09 re-run). Only remaining item: final location_missing copy sign-off (in-code TODO). |
| ING-09 | `POST /internal/reply` handler wired | [x] | Ali/Qoder, 2026-08-29 — reply server live on :3000; synthetic POST with a real sender_hash → template rendered + delivered to the Telegram chat. Core's pipeline will call it for real once readback lands (Day 4). |
| FE-05 | Filter chips (urgency/district/time), client-side filtering | [x] | Ali/Qoder, 2026-08-30 — real filtering logic implemented. `activeUrgency` (from map filter bar) and `activeQueue` (from left rail) are independent state vars; `ticketMatchesFilter()` applies both. `applyFilters()` re-renders the stream and calls `refreshPins()` in map.js. `addTicket`/`updateTicket` both route through `applyFilters` so live SSE tickets also respect the active filter. Chip count badges update on every ticket event. |
| FE-06 | Urgency colour ramp on pins/rows | [x] | Ali/Qoder, 2026-08-30 — `URGENCY_COLOURS` map in `map.js` drives `fillColor`/`color` on every `circleMarker`: vermilion (critical), marigold-bright (high), indus (moderate), silt (info). Matches CSS token values. `refreshPins(visibleTickets)` added for filter-driven pin redraw. Rich tooltip shows district, urgency label, P-code. `pinMap` keyed by ticket id for future FE-09 state updates. |
| DATA-05 | Degraded-audio ladder script (`degrade_audio.py`) | [x] | Alisha, 2026-08-30 — real ffmpeg `amix` SNR mixing (reuses `pipeline/preflight.py`'s volume/duration helpers rather than duplicating them). Noise bed is synthesized via ffmpeg's built-in `anoisesrc` (no download/licensing question) as a dev/test stand-in — a real rain/wind recording will be more representative for the actual `DATA-09` ladder run later. Used directly to test `CORE-13`'s gate at -10dB and -25dB SNR |
| — | **Sync point:** confirm `audio_unintelligible` truly skips the LLM | [ ] | Confirmed from the CORE side (Alisha) — degraded audio reliably reaches `audio_unintelligible` without ever calling `extract()`. Leaving unchecked until the full path is confirmed together, including the actual outbound WhatsApp/Telegram reply |

## Day 4 — Intelligence and standards

| ID | Task | Done | Owner / Notes |
| :-- | :-- | :--: | :-- |
| CORE-16 | Multi-intent extraction + fallback-model retry | [x] | Pulled forward, done as part of `CORE-08` — `extract_with_fallback()` already built and tested (both directly and via `CORE-09`'s live pipeline run). Multi-intent confirmed live: "Larkana aur Shikarpur" → 2 correctly separate tickets |
| CORE-17 | Glossary loaded into system prompt at startup | [x] | Alisha, 2026-08-30 — `{GLOSSARY}` placeholder in `system_extract.txt`, formatted from `prompts/glossary.json` (one source of truth now, not two), composed once and cached, warmed explicitly at app startup |
| CORE-18 | `pipeline/geocode.py`: exact → alias → fuzzy → none | [x] | Alisha — real alias/exact/fuzzy/none cascade against the actual gazetteer+aliases. Tested 8 scenarios incl. Urdu-script alias, case/whitespace tolerance, fuzzy typos, and a genuinely interesting finding: a misspelling *plus* a noise word ("Dadoo Distrct") can defeat both alias and fuzzy matching and correctly falls to "none" rather than guessing. **Update 2026-09-05, tehsil-level precision + a real pre-existing bug found and fixed:** Ali proposed indexing individual tehsils (admin-3) by their own coordinates so a message naming a specific town (e.g. "Mian Channu") drops a pin on that town instead of the coarser district centroid -- good idea, implemented, but his snippet's collision guard only protected a tehsil from overwriting its OWN parent district, not an unrelated one. Checked the real 577-row gazetteer before shipping it: 3 genuine collisions exist -- "Sahiwal" is both a real district AND an unrelated tehsil of Sargodha; "Khanpur" and "Kingri" are tehsil names shared across 2-3 different districts each. Fixed `build_district_index()` to skip indexing any tehsil name that collides with a real district name or with another tehsil in a different district (falls back to the district-level entry instead, which is unambiguous). **While testing this, found a separate, more serious PRE-EXISTING bug, already live in production, unrelated to today's feature**: `_try_cascade()` checked `data/aliases.json` *before* an exact district-name match, and that alias table (1287 entries, built from the gazetteer's tehsil rows) already contained the same 3 ambiguous tehsil names as aliases -- meaning a message saying the real district "Sahiwal" was *already* being silently misrouted to Sargodha in the deployed system, confirmed by testing against the actual pre-fix code. Fixed by reordering the cascade to exact-before-alias (a literal match to a real district name is strictly more certain than a heuristic alias-table guess) and removing the 3 ambiguous alias entries from `data/aliases.json` outright -- swept all 1287 entries afterward and confirmed no other alias key collides with a real district name. Verified: Mian Channu/Kabirwala now resolve to distinct real tehsil coordinates within Khanewal (not the district centroid); Sahiwal/Dadu resolve correctly at district level; Khanpur/Kingri (genuinely ambiguous, no other context) correctly fall to `none` rather than guessing one of several real districts; Muzaffargarh/D.I. Khan/Shaheed Benazir Abad (previously-fixed cases) unaffected. Documented as a `docs/TRD.md` 4.5 addendum since it changes the documented cascade order. |
| CORE-19 | `pipeline/urgency.py`: blended max score | [x] | Alisha — single-term rules + the "child/elderly + water-rise" compound rule. Tested 7 cases incl. confirming rules can escalate (model=info→critical) but never de-escalate (model=critical/high stays put even when rules say info) |
| CORE-20 | `pipeline/dedupe.py`: key-based near-duplicate flag | [x] | Alisha — bucket-key lookup against real SQLite. Tested: same district+intent+item within 15min correctly flags `duplicate_of`; different item/district don't match; Unlocated tickets never match (NULL adm2_name can't equal NULL in SQL, by design not oversight) |
| CORE-21 | Real `GET /api/export/hxl.csv` | [x] | Alisha — tested, two-row header confirmed correct (human-readable + HXL hashtags), one row per item |
| CORE-22 | Readback trigger on medium/high confidence | [x] | Alisha, 2026-08-30 — fires on `extraction_confidence >= 0.6` (a judgment call, TRD doesn't give an exact cutoff -- noted as worth revisiting once the gold set exists) AND a resolved location. `readback_sent_at` guards against resending. Confirmed live: fires, attempt logged (fails gracefully with no ingest/ running) |
| CORE-23 | Handle `1`/`2` replies → confirmed/disputed | [x] | Alisha — needed a new endpoint (`POST /internal/readback-reply`), documented as a TRD §3.1 addendum since the original contract never said how a bare "1"/"2" maps to a ticket id. Tested: confirm → `user_confirmed`, dispute → `user_disputed`, replying twice is a no-op (already-answered ticket no longer "pending"), reply from an unrelated sender no-ops cleanly |
| ING-10 | Route `1`/`2` control keywords to readback handler | [x] | Ali/Qoder, 2026-08-30 — `handleControlKeyword()` in `ingest/consent.js` now POSTs to `POST /internal/readback-reply` for bare `1`/`2` texts and returns true (never falls through to triage). Failed core POSTs are logged, not thrown. Verified: endpoint returns `{ok:true,ticket_id:null}` for unknown sender (correct — no pending readback). |
| FE-07 | Detail drawer (audio, transcript, fields, confidence, verdicts) | [x] | Ali/Qoder, 2026-08-31, audio verified 2026-09-01 — drawer opens/closes, renders extracted fields, confidence waveform, reasoning, transcript, and verdict buttons. Audio player verified live: Islamabad voice-note ticket #18 plays its WAV from core's `/audio` static mount (backend join + mount shipped by Alisha 2026-08-31); live SSE tickets backfill message fields lazily on drawer open (get_ticket SSE payloads don't join messages). |
| FE-08 | TTT header (median/p95/baseline/queue depth) | [x] | Ali/Qoder, 2026-08-30 — `pollMetrics()` fetches `GET /api/metrics` every 5s, formats ms values as `Xs` or `Xms`, writes into `#ttt-summary` with all four fields (median/p95/baseline/queue). Verified live: `{median_ttt_ms:6269, p95_ttt_ms:6269, human_baseline_ms:29940, queue_depth:0}` rendered correctly. Silently no-ops if core is down. |
| FE-09 | Pin/state shape semantics | [x] | Ali/Qoder, 2026-08-31 — confirmed/unconfirmed/disputed shapes wired to verification status + dispatcher_verdict; `updatePin()` re-renders single pin in place on SSE `ticket.updated`; colors now read from CSS tokens. Verified live: Acknowledge → pin flips solid. |
| DATA-06 | HXL export validated against spec | [x] | Re-validated 2026-08-30 with real Day-4 pipeline output (multi-district, Unlocated, duplicate cases all present) — two-row header still correct, blank fields render cleanly for Unlocated rows. UI path verified 2026-09-01 (DoD-9 pre-check): export button fetches a valid CSV (200, text/csv, 10 human columns + 10 HXL hashtags, 27 rows incl. same-day tickets); `exportHXL()` hardened to anchor+download after `window.open` was identified as a popup-blocker demo risk. |
| — | *(unscheduled)* DoD pre-verification runs on dev machine (formal gate stays REL-07 fresh-clone) | [x] | Ali/Qoder, 2026-09-01 — DoD-3: -15dB-SNR degraded real voice note → `audio_unintelligible`/`STT_LOW_CONFIDENCE`, 0 tickets, LLM never called, status card rendered, outbound reply fired to ingest. DoD-5: one 3-district text (Larkana/Sukkur/Multan) → 3 tickets, 3 P-codes (PK713/PK723/PK622), correct provinces, exact geocode. DoD-9: HXL endpoint + UI button verified (see DATA-06). DoD-8 evidence gathered under FE-12. Remaining unproven on dev: DoD-1 (fresh-clone TTT), DoD-2 partially (WhatsApp-side consent already live-verified; BAND purge verified via Telegram), DoD-6 second half ("2" → disputed on real WhatsApp), DoD-10 (airplane-mode full demo). |
| — | *(unscheduled)* Audio player backend support for `FE-07` | [x] | Alisha, 2026-08-31 — the two changes Ali/Qoder's `FE-07` note flagged as blocking it: `db.py`'s `list_tickets()` now `JOIN`s `messages` for `modality`/`audio_path`/`audio_duration_s`/`raw_text` (INNER JOIN is safe -- `message_id` is `NOT NULL` + FK-enforced), and `/audio` is mounted in `main.py` onto `storage/audio/`. Tested: static mount serves a real file (200) and 404s a missing one; `GET /api/tickets` confirmed carrying real `audio_path`/transcript for both a text ticket (null audio fields, as expected) and a real ingested audio ticket (real path + real transcript) -- not just tickets that arrive live over SSE while the dashboard happens to be open, which was the actual bug |
| — | **Sync point:** correction loop end to end, together | [x] | Verified live on real WhatsApp (Green API transport), 2026-08-31 — voice note (Dadu, water/food) → STT → extract → geocode (exact, PK703) → urgency high → readback message sent → sender replied "1" → ticket #7 `verification_status: user_confirmed` in DB. CORE side confirmed by Alisha (readback trigger + `POST /internal/readback-reply`); ingest side (ING-10 routing "1"/"2") confirmed live by Ali/Qoder |

## Day 5 — Polish and proof

| ID | Task | Done | Owner / Notes |
| :-- | :-- | :--: | :-- |
| CORE-24 | Hardening pass across full error taxonomy (TRD §9) | [x] | Alisha, 2026-08-30 — audited every TRD §9 row against actual code. Found 2 real gaps and fixed both: (1) `STT_REPETITION_LOOP` never fired, every gate failure was collapsed into generic `STT_LOW_CONFIDENCE` — split `stt.py`'s gate into `gate_failure_reason()`, tested all 4 classification cases incl. both-conditions-present (repetition wins, it's more specific); (2) exhausted-retry Groq failures (rate limits, outages) fell through to the queue's generic `PIPELINE_ERROR` instead of the taxonomy's `RATE_LIMITED` — added `try/except groq.APIError` around both the STT and extraction call sites, tested with a simulated persistent 429. `AUDIO_TOO_SHORT/LONG/SILENT`, `EXTRACTION_INVALID` already confirmed Day 2/3. `GEOCODE_NO_MATCH` is represented via `ticket.geocode_method='none'`, not a `messages.error_code` — geocoding is per-ticket, not per-message, so it doesn't belong on that column. Full regression passed after both fixes |
| CORE-25 | `POST /api/simulate` behind `DEMO_MODE` | [x] | Alisha, 2026-08-30 — revisited now that `CORE-09` landed: no longer inserts a canned ticket, runs the real `_run_pipeline_sync` (same code path as `/internal/ingest`), optional `{"text": "..."}` body overrides the default message. Tested: default text → 1 ticket, custom multi-district text → 2 tickets, both correctly Unlocated (no geocoder yet, matches TRD §10 "nothing about the demo is faked") |
| ING-11 | Reconnect handling (restartRequired / loggedOut) | [x] | Ali/Qoder, 2026-09-01 — implemented + verified for the greenapi transport (Baileys' restartRequired/loggedOut states don't exist in an HTTP-polling transport; the equivalent failure modes are network outage and instance expiry). Poll loop now backs off exponentially on consecutive failures (3s → 6s → 12s → 24s → 48s → 60s cap), resetting on the first successful poll; 401/403 logs one actionable warning per streak ("re-scan the QR at console.green-api.com"). Verified: (1) live sustained-outage test against a nonexistent instance id — real 403 streak showed the 3/6/12/24s ladder, single auth warning, 5 requests in 45s vs 15 under the old flat-3s loop; (2) mocked transient outage — fail, fail, succeed, succeed, fail logs "1 in a row" again, proving the reset; (3) earlier live evidence: 2 transient `fetch failed` errors during real operation, recovered with no lost messages. |
| ING-12 | Ban-safety pacing confirmed under burst test | [x] | Ali/Qoder, 2026-09-01 — 12-message instant-enqueue burst through the real `OutboundQueue` with a mocked transport: 12/12 delivered, every inter-send gap inside the 1.5–3.0s jitter window (measured 1691–3034ms), strictly serial (max concurrent sends = 1) — the wire never sees more than one message per ~1.7s no matter how hard the queue is hit. Inbound side: 1 poll/s is Green API's documented HTTP-API pattern, and as an official API gateway (vs Baileys' unofficial protocol) the ban-risk model is per-instance rate limits, not protocol detection. Combined with DATA-08's core-side 50-message burst pass. Honest scope note: a true 50-message burst over live WhatsApp (50 real sends from real phones) was not performed — the queue-level proof plus the gateway argument is the recorded evidence. |
| FE-10 | Offline OSM tile cache for demo bounding box | [x] | Ali/Qoder, 2026-09-01 — `tools/fetch_tiles.py` downloaded 232 tiles (zoom 6–9, lat 23.5–31.5 / lon 66.5–74.0, Sindh + South Punjab incl. Bahawalpur division; 3.2MB, idempotent re-runs, PNG-validated, paced per OSM tile policy) into `dashboard/tiles/` (committed, so a fresh checkout renders offline). `map.js` probes the cache at boot (Image load of the z6 tile under the default centre — works under http: and file:) and serves local tiles with `maxNativeZoom: 9` (upscaled to 11); falls back to live OSM when absent. Browser-verified: tile srcs from `localhost/tiles/`, 4/4 loaded, pins intact. Script tags versioned `?v=2` because `http.server` sends no cache headers and a plain F5 was serving stale JS. *Superseded 2026-09-01 by the image-2 redesign: cache rebuilt as 596 CartoDB Positron tiles, bbox lat 22.5–33.5 / lon 62.0–78.0 — see the redesign row below.* |
| FE-11 | Empty/error states (UI-UX §8) | [x] | Ali/Qoder, 2026-08-31, genuine card verified 2026-09-01 — `message.status` failure events (`preflight_failed`, `audio_unintelligible`, `failed`) render marigold status cards in a dedicated `#status-stack`; cards persist across ticket filter changes and respond to queue filters. Earlier bug that rendered `transcribed`/`extracted` as failure cards fixed. The previous caveat (genuine `audio_unintelligible` card never observed live) is resolved: a -15dB-SNR degraded real voice note was injected, message hit `audio_unintelligible`/`STT_LOW_CONFIDENCE` with zero tickets, and the card rendered live over SSE with title, subtitle, error-code sub-line and 🔇 icon; Unhearable queue filter shows it with no misleading empty-state. |
| FE-12 | SSE reconnect / `Last-Event-ID` replay verified | [x] | Ali/Qoder, 2026-09-01 — verified at three levels. (1) Protocol: curl disconnected at event seq 63, a new message flowed through the pipeline while disconnected, reconnect with `Last-Event-ID: 63` replayed exactly the missed events (64–66) once, then live `metrics` continued — no full-history re-send on reconnect. (2) Fresh-load dedupe: core replays ALL events from seq 0 on every fresh SSE connection, which had been silently duplicating every ticket card on every page load (REST load + full replay, `addTicket` blindly unshifted); replayed `ticket.created` for held ids is now ignored (created payload is always the oldest state; `updateTicket` converges to current). Browser-verified: 19 unique cards, ids 1–19 exactly once, live indicator "live". (3) Status cards made replay-idempotent (replayed failure events replace their card, don't stack, don't re-count the Unhearable badge). Native EventSource auto-reconnect observed retrying while core was down. |
| — | *(unscheduled)* Image-2 dashboard redesign: two-column shell, live voice cards, TTT arc speedometer, Positron map | [x] | Ali/Qoder, 2026-09-01 — full visual overhaul to the image-2 target, driven by the "not hearable or clickable" complaint on the old unintelligible card. Shell: slate #F8FAFC page, white cards, header row (brand + TTT arc + live + export) over map-hero / 440px Live Feed columns; 1080px responsive fallback stacks map over feed; 768px mobile view with sticky view switcher tabs (`[🗺️ Map]` / `[📋 Feed]`) and bottom sheet drawer. Voice cards (`renderTicketCard` rewritten): district + urgency + timestamp header, **playable waveform** via `player.js` VoicePlayer, structured summary, dual-script transcript (Urdu script vs English Translation tab), relief supply badges, enlarged dismiss button (`.btn-card-dismiss`), Acknowledge → "✓ Acknowledged". TTT header (FE-08) is now a compact 2-line horizontal pill with 120° arc speedometer. Map (FE-06/FE-10): basemap switched to **CARTO Voyager** with authorized API key (`cb1_2qam_1_e831351c560f2413f1d51b3b`) and 596 cached offline tiles; map pins now have interactive click listeners opening the detail drawer and scrolling to the card. Live District Needs Ticker aggregates real-time localized supply tallies. Audio alert chime (Web Audio API) fires on critical tickets with mute toggle. Full keyboard shortcuts (<kbd>A</kbd>, <kbd>D</kbd>, <kbd>Esc</kbd>, <kbd>J</kbd>, <kbd>K</kbd>). Status cards upgraded with inline `<audio controls>` for `audio_unintelligible` events. Persistent client-side ticket dismissal added (`localStorage`), and new **Acknowledged** queue tab added. Typography upgraded to **Plus Jakarta Sans** + **Noto Nastaliq Urdu / Gulzar**. |
| DATA-07 | Gold set finished (25 msgs, 8+ real audio recordings) | [~] | Ali/Qoder, 2026-09-01 — scaffold complete and committed: `tools/build_gold_set.py` holds all 25 case specs as the single source of truth and validates every label on each run (intent/urgency vs core/pipeline/extract.py Literals; every P-code vs pak_gazetteer.csv — 25/25 clean), `data/gold_set/cases/` carries the generated label.json files, `data/gold_set/README.md` documents the recording recipe. Remaining manual work: the 10 real voice notes (a01–a10, script is in each case's label.json) — Ali records; a11–a15 severe tier via the README's edge-tts + degrade_audio recipe; a06–a10 moderate-tier noise mixing per README. |
| DATA-08 | Burst test: 50 concurrent, zero crashes/locks/429s | [x] | Alisha, 2026-08-30 — `tools/burst_test.py` built for real (was a stub) and **PASS**: 0 5xx, 0 DB-lock errors, 50/50 message rows, queue fully drained, 0 messages stuck. 2 of 50 landed on `RATE_LIMITED` under the 25 RPM LLM bucket — that's the *handled* case (`CORE-24`'s fix working correctly under real load: retried, then failed gracefully with a specific reason), not a test failure; TRD §8 asks for zero *unhandled* 429s. First run's fixed-sleep wait produced a false "FAIL" (queue was still draining, not stuck) — rewrote to poll `queue_depth` instead of guessing a wait time |
| DATA-09 | Acoustic degradation ladder run, SNR give-up point recorded | [~] | Alisha, 2026-08-30 — built `tools/acoustic_ladder.py`, ran it against 1 synthetic sample (edge-tts + synthetic pink noise, no real rain/wind recording yet). **Result: primary model (`whisper-large-v3-turbo`) gate reliably fails at ≤-10dB SNR; with escalation, the true system give-up point is ~-8 to -10dB**, with some run-to-run variance right at the boundary (the noise generator isn't seeded, so exact pass/fail near the threshold isn't perfectly repeatable — expected for a boundary condition, not a bug). Notable qualitative finding: under heavy noise Whisper doesn't just fail cleanly, it hallucinates short generic words ("موسیقی"/music, "شکریہ"/thanks) — confidently wrong, not obviously wrong. **This is a working ladder + a first real number, not the formal TRD §8 result** — that needs the actual 5 clean gold-set samples (`DATA-07`, not done yet) remixed with real rain/wind, not synthetic pink noise on ad-hoc text. Re-run against the real gold set once it exists; don't quote this -8/-10dB number as final |
| DATA-10 | Gold-set accuracy computed, sample size reported | [ ] | Ali/Qoder, 2026-09-01 — harness complete and committed (`tools/gold_eval.py`): injects each case through the real pipeline, polls read-only to terminal status, scores exact district+intent per TRD §8 with n beside every number, records per-ticket TTT, writes `data/gold_set/eval_report.json`. Text-half pre-run (informal, n=10): district 0.9, intent 0.6, TTT median 2542 ms / p95 4503 ms — text comfortably under the 5 s DoD bar. Four intent misses diagnosed for Person A (labels stand; all fixes are core-side prompt work): (1) t01 Multan "log pareshan hain… madad pohnchao" → non_actionable, should stay actionable — same behaviour twice, the DoD-5 run agrees; (2) t02 one no-location message over-split into two identical (None, resource_request) records — the human answer is one ticket; (3) t07 infrastructure_damage vs incident_report for "power+network down, road flooded" — taxonomy ambiguity, needs a joint ruling on the label; (4) t09 Badin "fishermen lost their boats" → resource_request — clear miss, a loss report is not a request. Formal number waits for the full 25-case run after the a01–a10 recordings. Alisha, 2026-09-01 — fixed t01/t02/t09 (plus a real t03 bug found along the way) in `core/prompts/system_extract.txt` + `glossary.json` + `data/aliases.json`. This took 3 iterations against the REAL gold-set cases (not reconstructions) because the first two fixes were each too broad and caused real regressions before landing on the final wording:
  - Rule 1: forbids splitting one sender's single situation into multiple records by symptom/clause (was causing t02-style over-splits) while still requiring genuinely separate locations/parties to split (kept t01/t09's legitimate multi-district splitting intact).
  - Rule 6a/b/c: a record's OWN explicit direct ask ("madad bhejein", "foran ao") makes it resource_request even alongside a hazard description (fixes t01, and t03 below); a record reporting something already lost/destroyed with no request of its own stays incident_report/infrastructure_damage (fixes t09); a generic closing appeal shared across multiple districts doesn't override a district's own specific incident content (this is what the 2nd iteration got wrong — first version let "dono jagah madad chahiye" flip Ghotki's rising-groundwater report to resource_request; tightened to require the request be that record's *own* point, not borrowed from elsewhere in the message).
  - **Real bug found via this testing, not part of the original 4**: t03 ("مظفرگڑھ" / Muzaffargarh) was geocoding to **Muzaffarabad** — a different real city in a different province. Root cause: `aliases.json` had a Roman-spelling alias for Muzaffargarh but no Urdu-script form, so `location_raw` (correct Urdu text) couldn't match anything, and the cascade fell back to the LLM's own `district_guess`, which had hallucinated "Muzzafarabad." Fixed by adding the missing Urdu-script alias — geocoder now resolves it correctly via `alias`, not the LLM's guess at all.
  - **Verified against the real harness** (`tools/gold_eval.py`, not my own reconstructions): 3 full runs of all 10 text cases — intent accuracy 1.0, 1.0, 0.8 (the one dip was `t07`, expected/untouched, plus a newly-surfaced `t05` — see below). t01/t02/t03/t09 individually confirmed 3/3 or 4/4 clean in isolation and combined.
  - **`t05` discovered as the same ambiguity class as `t07`, not a separate bug**: "Rajanpur mein bund tootne se teen log beh gaye... ghayal hain" (embankment broke, 3 swept away, injured) intermittently misclassified as infrastructure_damage instead of incident_report — same structure-caused-vs-people-consequence tension as t07's "power+network down, road flooded." Deliberately did NOT patch this with a 4th one-off rule; two whack-a-mole regressions already happened this session from narrow patches, and both t05 and t07 should resolve together once the joint ruling below is made.
  - **t07/t05 resolved, 2026-09-02** — you and Ali agreed on the casualty-based tiebreaker: a record mentioning any casualty/injury/missing/trapped/swept-away person is `incident_report` regardless of what caused it (a broken embankment, a collapsed bridge); `infrastructure_damage` is reserved for damage reports with no such person-harm content. Added as rule 6d in `system_extract.txt`, with both real cases as worked examples. **Verified: `t05` + `t07` both PASS, 6/6 across 3 clean isolated runs** (`tools/gold_eval.py t05 t07`, run 3x).
  - **Full 10-case regression blocked by real Groq daily quota exhaustion, not a bug**: hit `openai/gpt-oss-120b`'s 200,000 TPD limit from today's testing volume (many gold-eval/burst-test/smoke-test cycles). Confirmed via direct API error, not assumed — and confirmed the quota is a genuine rolling window, not a quick reset: a retry ~5 min later let exactly one case through before hitting the wall again. Every case that *did* hit the wall correctly landed on `status=failed`/`error_code=RATE_LIMITED` (live confirmation the `CORE-24` hardening from Day 5 works under a real exhaustion, not just a simulated one) — nothing crashed, nothing silently lost. **Formal 10-case (and eventually 25-case) accuracy number still needs a full clean run once the quota window clears** — worth running early in a session, before other heavy Groq usage, next time. t01/t02/t03/t09/t05/t07 are all individually re-confirmed multiple times each today; the only thing genuinely unconfirmed is whether all 10 pass together in the *same* run, which is a fair thing to want before quoting a final number. |
| — | **Sync point:** full test suite run together at least once | [ ] | |

## Day 6 — Freeze and rehearse

| ID | Task | Done | Owner / Notes |
| :-- | :-- | :--: | :-- |
| — | *(unscheduled)* Render deployment: Postgres backend, cross-service audio forwarding, Blueprint | [x] | Alisha, 2026-09-05 — three real gaps found and closed while implementing the "deploy everything to Render, including ingest/" request: (1) `core/db.py` now supports Postgres via `DATABASE_URL` (unset = SQLite, unchanged) alongside SQLite, pulling forward the Postgres half of `docs/ARCHITECTURE.md`'s already-documented Phase 3 roadmap item -- same DAO function signatures, dual schema DDL, a thin `?`->`%s`/dict-row shim so nothing calling into `db.py` changed except the 3 `lastrowid` call sites (now `RETURNING id`/`RETURNING seq` on Postgres); SQLite path regression-tested clean, Postgres path is code-complete but untested against a live instance (no Docker/Postgres available locally -- first real test will be against Render's managed Postgres). (2) Found and fixed a real architectural gap: `ingest/` and `core/` as separate Render services don't share a disk, so a voice note's `audio_path` (valid on ingest's filesystem) was meaningless to core's pipeline. Fixed by forwarding the audio bytes themselves (`audio_data_b64`/`audio_filename`) in `/internal/ingest`; core saves its own copy and reads that -- verified live end to end (base64 round-trip, file written to core's `storage/audio/`, served correctly via `/audio`, pipeline read the *local* saved path not ingest's foreign one). Documented as a `docs/TRD.md` 3.1 addendum (contract change, hard rule 1). (3) `ingest/index.js`'s reply-server port was hardcoded from `INGEST_URL`'s own port, which breaks under Render's dynamically-assigned `$PORT` -- fixed to prefer `process.env.PORT`. Also added a real `GET /health` route (was a bare 404) that doubles as an external-pinger keepalive target, since research surfaced a genuine blocker worth flagging: Render free tier spins a service down after 15 min with no *inbound* traffic, and `ingest/`'s Green API polling loop is outbound-only, so free-tier `ingest/` silently stops receiving WhatsApp messages after 15 idle minutes unless something pings it or it's upgraded to a paid instance -- documented in README's new "Deploying to Render" section, not silently glossed over. `render.yaml` Blueprint provisions all 4 resources (Postgres + 3 services); schema verified against Render's own current Blueprint docs before writing (field names like `runtime:` vs the deprecated `env:`, static-site `runtime: static`, `rootDir`, `disk`). Investigated but did **not** implement the literal "nested CORSMiddleware around the audio mount" ask -- empirically confirmed (curl with `Origin: null`, what `file://` sends) that the existing Day-1 global `CORSMiddleware` already covers it; a second nested one risked a duplicate-header bug for zero benefit. **Update 2026-09-05, now fully verified live, not just code-complete:** the initial Blueprint sync failed once (`fromService` needs an explicit `type: web` field that Render's error message named directly -- not documented in the page fetched beforehand, fixed immediately once seen) and `nidaa-core` itself got Render-assigned a suffixed URL (`nidaa-core-sgrm.onrender.com`, not the plain `nidaa-core.onrender.com` guessed in the first pass) -- both `CORE_URL`/`INGEST_URL` cross-service links were switched from hardcoded public-URL guesses to `fromService` private-network references (self-healing against future suffix changes), and the dashboard's build-time `NIDAA_CORE_PUBLIC_URL` (which can't use `fromService` since a browser can't reach Render's private network) was set manually once. All four resources are live: `nidaa-core` boots clean and connects to the managed Postgres (first real confirmation the Postgres schema/DAO code actually works, not just passes a local SQLite regression test), `nidaa-ingest` is polling Green API successfully, `nidaa-dashboard` resolves the right core URL and its SSE connection shows "live". **End-to-end proof:** `POST /api/simulate` against the deployed `nidaa-core` produced a real ticket (Dadu, PK703, critical, 20 khana/20 pani) through the full live pipeline -- Postgres insert, real Groq extraction+geocoding+urgency call, SSE push -- rendered correctly on the deployed dashboard at 2.7s TTT. **Update 2026-09-05, real WhatsApp voice note confirmed end to end**, plus one real bug found and fixed along the way: `ingest/consent.js` reads `CORE_URL` independently from `index.js` (its own top-level `const`), and only `index.js`'s copy had the `withScheme()` fix from the first Render pass -- so on Render, `checkConsent()`'s fetch got a scheme-less URL and failed on every real inbound message with a bare "fetch failed" (never caught by the `/api/simulate` test, which never touches `ingest/` at all). Fixed by extracting `withScheme()` into a shared `ingest/url.js` both files import, and improved the swallowed-error log (`err.cause`) that had been hiding the real reason. That fix then surfaced a second, more fundamental issue: `core/main.py`/`ingest/index.js`'s `fromService` (private-network) cross-links for `CORE_URL`/`INGEST_URL` don't work at all on Render's free tier -- confirmed via Render's own docs, "Free web services can send private network requests, but they can't receive them," so `nidaa-core` (free) structurally cannot receive `nidaa-ingest`'s private-network calls regardless of DNS/config. Reverted both to plain public HTTPS URLs in `render.yaml` (`nidaa-ingest`'s own URL is stable and hardcoded; `nidaa-core`'s carries an unpredictable name-collision suffix, so `CORE_URL` on `nidaa-ingest` is a manual `sync: false` value instead, same pattern as the dashboard's `NIDAA_CORE_PUBLIC_URL`). **Full real-audio test after that fix**: a real voice note ("Jadoo/جدو mein bees gharon ke liye khana aur pani chahiye, log phansay huay hain") went through Green API -> ingest (real audio download + base64 forward) -> core (STT, transcribed correctly) -> extraction (20 food/20 water, resource_request, correctly identified) -> urgency correctly escalated to critical on "log phansay huay hain" (trapped-person compound rule) -> consent notice fired -> geocoding correctly returned `none` (the spoken place name doesn't match any real gazetteer district -- hard rule 3 working as designed, not a bug) -> ticket rendered Unlocated on the deployed dashboard -> `location_missing` WhatsApp reply fired asking for a live location or a real place name. Every stage of the real pipeline confirmed live on the deployed stack with real audio, for the first time. Not yet tested: a voice note naming a real gazetteer district (would confirm the full geocoded-ticket + readback-confirmation `1`/`2` loop on the deployed stack specifically, though this exact loop was already verified pre-deployment). `DEMO_MODE=true` remains on and `/api/simulate` remains unauthenticated (PRD §13's stated MVP limitation) -- accepted for now, revisit before demo day. |
| REL-01 | Feature freeze at noon | [ ] | |
| REL-02 | 90-second insurance video recorded | [ ] | |
| REL-03 | Full demo run in airplane mode + hotspot | [ ] | |
| REL-04 | `DEMO_MODE` simulator fallback verified standalone | [ ] | |
| REL-05 | Demo rehearsed 3x, at least once fully offline | [ ] | |
| REL-06 | Pitch deck built around the measured TTT number | [ ] | |
| REL-07 | Definition of Done checklist passed on a clean checkout | [ ] | |

---

## Definition of Done (TRD §12) — final gate

Check these only on a **fresh clone**, not your dev machine's warm state.

- [ ] 1. Real-phone voice note → mapped, HXL-tagged ticket, median TTT < 5s
- [ ] 2. Consent notice fires exactly once per sender; `BAND` stops + purges
- [ ] 3. Noise-destroyed voice note → orange `AUDIO_UNINTELLIGIBLE`, LLM never called
- [ ] 4. No-location message → null-coordinate ticket in Unlocated queue, no invented coordinates anywhere
- [ ] 5. Three-district message → three separate tickets, three P-codes
- [ ] 6. Readback fires; replying `2` → disputed + top of review queue
- [ ] 7. 50-message burst: zero crashes, zero DB locks, zero unhandled rate-limit errors
- [ ] 8. 30s browser disconnect → identical state on reconnect
- [ ] 9. HXL CSV export opens cleanly with a valid hashtag row
- [ ] 10. Full demo runs from the simulator with WhatsApp fully disconnected
